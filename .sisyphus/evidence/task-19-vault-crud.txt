## T19 — Vault / Organization / Sync Evidence

Date: 2026-05-05

### Runtime proof

Command:

`node test/run-vault-crud-e2e.mjs`

Result:

- 4/4 passing
- Vault CRUD happy path: PASS
- Org creation and shared vault: PASS
- Unauthorized vault access rejected: PASS
- Idempotent retry / OUTDATED_REVISION: PASS

### Key flow notes

- The Worker harness now mirrors the real Padloc auth flow validated in T18.
- Shared-vault creation is a two-step flow, matching `packages/core/src/app.ts#createVault()`:
  1. `api.createVault()` creates the shared vault in the org context.
  2. A follow-up org update assigns vault access to the owner/member before retrieval.
- Unauthorized reads still return `NOT_FOUND`, matching `packages/core/src/server.ts#getVault()` semantics.
- Vault payloads remain opaque encrypted blobs; the server never decrypts them.

### Files exercised

- `packages/worker/test/vault-crud-e2e.worker.ts`
- `packages/worker/test/run-vault-crud-e2e.mjs`
- `packages/core/src/server.ts`
- `packages/core/src/app.ts`

### Acceptance summary

- Authenticated user can create/read/update/delete encrypted vaults: YES
- Shared/org vault flow works with correct owner assignment semantics: YES
- Unauthorized cross-account access is rejected: YES
- Duplicate stale revision update returns `OUTDATED_REVISION`: YES
